on:
  push:
    tags:
      - v*

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/attest@v4
        with:
          subject-path: README.md
